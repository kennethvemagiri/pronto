{
  "name": "pronto-online",
  "version": "1.0.1",
  "description": "Local-first prompt polisher (Express + static UI + Ollama)",
  "main": "apps/api/server.js",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "scripts": {
    "start": "node apps/api/server.js",
    "dev": "node apps/api/server.js",
    "build": "npm audit --production",
    "test": "echo 'Tests not yet configured' && exit 0",
    "lint": "echo 'Linting not yet configured' && exit 0"
  },
  "keywords": [
    "prompt",
    "ollama",
    "ai",
    "enhancement",
    "nlp"
  ],
  "author": "Pronto Team",
  "license": "MIT",
  "dependencies": {
    "@supabase/supabase-js": "^2.100.1",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^8.3.1",
    "openai": "^6.37.0"
  }
}
